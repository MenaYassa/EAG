# EAG G2.4 — Governed Engineering Execution Loop

**Design date:** 21 August 2026
**Audited baseline:** [`v2.3.2-g2.3.2`](https://github.com/MenaYassa/EAG/tree/v2.3.2-g2.3.2) at `84d745314f71d532c89700bc89340412a114f78d`
**Status:** **G2.4.1 state-machine foundation, G2.4.2 trusted verification boundary, and G2.4.3 bounded reflection/replanning evidence contracts are implemented and validated. G2.4.4–G2.4.5 are not started.**
**Author:** Manus AI

> **Safety thesis — RECOMMENDATION.** G2.4 should not turn a plan, reflection, or LLM response into an execution authority. It should add a deterministic execution-state controller that selects the next permitted operation from typed evidence, while preserving the existing `EngineeringDecision → ChangeProposal → policy → one-time authorization → atomic mutation → receipt` boundary for every individual mutation.

## 1. Executive Summary

**FACT.** G2.3.2 provides a narrow, public, one-shot workflow: a governed gateway returns an `EngineeringDecisionResult`; the translator derives at most one `ChangeProposal`; and `GovernedMutationRuntime` applies the existing G2.3.1 policy, authorization, atomic write, and receipt path. The workflow has terminal failure classification, but it neither loops nor replans. See `src/eag/chief/intelligence/gateway/mutation_workflow.py`.

**FACT.** EAG separately has an `AutonomousLoopRuntime` that iterates a legacy Chief/Coordinator plan-execution path, reflects on the result, stores reflection memory, and uses a deterministic completion engine to stop, continue, replan, abort, or request approval. The canonical autonomous factory does **not** compose repository context assembly, the governed gateway, or the G2.3.2 mutation workflow. See `src/eag/autonomous/runtime.py` and `src/eag/autonomous/factory.py`.

**INFERENCE.** The missing capability is not merely “run twice.” The repository lacks a persistent, deterministic, governed execution ledger that binds each iteration’s repository evidence, decision, proposal, authorization, mutation receipt, objective verification, reflection, budget consumption, and terminal reason.

**RECOMMENDATION.** G2.4 should introduce a new governed-loop composition rather than redirecting the existing generic `CapabilityRuntime` path into mutation. The first increment should be a state-machine and immutable execution-context contract; it should not yet make arbitrary plans, generic capability metadata, or LLM outputs executable.

| Topic | Current evidence | G2.4 design conclusion |
|---|---|---|
| Single mutation | **FACT:** G2.3.2 composes exactly one gateway decision, one proposal, and one receipt. | **RECOMMENDATION:** Preserve one proposal and one authorization per governed iteration. |
| Existing autonomous iteration | **FACT:** `AutonomousLoopRuntime` owns max-iteration looping, reflection, memory, completion, recovery, and approval. | **RECOMMENDATION:** Reuse selected concepts, not its generic capability execution path, for G2.4. |
| Objective verification | **FACT:** mutation postcondition verifies exact written content; legacy `DefaultValidator` only evaluates generic step success and may retry. | **RECOMMENDATION:** Add a first-class deterministic engineering verification contract. |
| Continuation control | **FACT:** current completion uses `RunResult`, reflection score, iteration count, and critical findings. | **RECOMMENDATION:** State transitions must be selected by deterministic transition guards over typed evidence and budgets. |
| Live acceptance | **FACT:** G2.3.2 implementation is complete, while EBS-015 live acceptance is pending. | **RECOMMENDATION:** G2.4 begins with deterministic fixtures and does not depend on a new live-provider call. |

## 2. Current G2.3.2 Baseline

**FACT.** The latest committed milestone is `84d745314f71d532c89700bc89340412a114f78d`, tagged `v2.3.2-g2.3.2`. The permanent G2.3.2 record distinguishes deterministic implementation completion from pending live EBS-015 acceptance.

**FACT.** `ChangeProposal` is immutable and carries `run_id`, `decision_id`, target path, operation, content fingerprint, precondition, expected postcondition, provenance, context fingerprint, repository snapshot fingerprint, and workspace fingerprint. `MutationAuthorization` is proposal-digest-bound and one-time. `MutationReceipt` is immutable, redacted, terminal, and records the mutation result and postcondition verification. See `src/eag/mutation/models.py`.

**FACT.** `GovernedMutationRuntime.execute` validates a proposal, authorizes it, revalidates it under a lock before consuming authorization, atomically writes one bounded file, verifies the fingerprint-based postcondition, conditionally rolls back on mismatch, and emits lifecycle events. See `src/eag/mutation/runtime.py`.

**FACT.** G2.3.2 adds a trusted `PreservationRequirement` to an opt-in mutation-intent policy. The provider declares requirement identifiers, while trusted translation rejects replacement content that omits a configured leading protected region. See `src/eag/chief/intelligence/gateway/models.py` and `mutation_translation.py`.

**FACT.** The G2.3.2 deterministic baseline was `3506 passed, 4 skipped`; targeted G2.3.2/G2.3.1/G2.2 tests were `89 passed, 2 skipped`; autonomous tests were `3 passed`; normal EBS tests were `7 passed, 3 skipped`; Ruff, MyPy, and whitespace checks passed.

**FACT.** The two authorized EBS-015 live observations do not establish live acceptance: attempt one reached a successful governed mutation but failed the exact fixture poststate under the earlier underspecified preservation contract; attempt two timed out before structured output. No further EBS-015 attempt is authorized by this design.

## 3. Current Architecture

### A. Current G2.3.2 governed mutation seam

```text
[FACT: separate, explicit composition only]

RepositoryContextAssembler (read-only; optional caller composition)
        |
        v
EngineeringDecisionRequest + EngineeringContext
        |
        v
GatewayRuntime.decide()
  route -> provider execution -> parse schema -> deterministic decision policy
        |
        v
EngineeringDecisionResult
        |
        v
DecisionToChangeProposalTranslator
  confined trusted reads + context/snapshot binding + preservation checks
        |
        v
ChangeProposal (untrusted, one file)
        |
        v
GovernedMutationRuntime
  validate -> authorize -> revalidate/consume -> atomic write -> postcondition
        |
        v
MutationReceipt (redacted terminal evidence)
```

**FACT.** `GatewayRuntime` emits routing, attempt, schema/policy, completion, and failure events. It performs provider routing and transport but returns a validated advisory result or safe failure; it does not write a workspace. See `src/eag/chief/intelligence/gateway/runtime.py`.

**FACT.** `GovernedDecisionMutationWorkflow.execute` calls the gateway once, translates the successful result once, and executes the resulting proposal once. Its outcome includes one gateway result and optional one proposal and receipt. It has no iteration, context refresh, memory write, reflection, or replanning loop. See `src/eag/chief/intelligence/gateway/mutation_workflow.py`.

### B. Current canonical autonomous build path

```text
[FACT: current CLI build composition]

CLI build(goal, workspace_dir)
        |
        v
LoopContext(goal, max_iterations=3)
        |
        v
create_autonomous_engineering_composition(workspace)
        |
        +--> WorkspaceRuntime + RepositoryRuntime
        +--> WorkspaceCapability + RepositoryCapability
        +--> CapabilityRuntime
        +--> DefaultPlanner + AdaptivePlanner
        +--> Coordinator + ChiefRuntime
        +--> ReflectionRuntime + MemoryRuntime
        '--> AutonomousLoopRuntime

AutonomousLoopRuntime iteration
        |
        v
ChiefRuntime.execute_goal(RunContext)
        |
        v
Coordinator.run
  DefaultPlanner -> optional AdaptivePlanner -> generic CapabilityRuntime
        |
        v
RunResult -> ReflectionRuntime -> MemoryRuntime -> CompletionEngine
        |
        +--> next Chief run / replan decision
        +--> wait for approval
        '--> completed / failed / aborted
```

**FACT.** `create_autonomous_engineering_composition` registers generic `WorkspaceCapability` and `RepositoryCapability` in `CapabilityRuntime`; it does not compose `RepositoryContextAssembler`, `GatewayRuntime`, `DecisionToChangeProposalTranslator`, or `GovernedDecisionMutationWorkflow`. See `src/eag/autonomous/factory.py`.

**FACT.** `Coordinator` converts every legacy `PlanStep` into a generic `CapabilityRequest` using `PlanStep.metadata`, then sends it to `CapabilityRuntime.execute`. See `src/eag/chief/runtime/coordinator.py`.

**FACT.** The `build` CLI command creates the canonical autonomous composition and calls `composition.loop.execute(LoopContext(...))`. The benchmark CLI independently wires the legacy generic capability path. See `src/eag/cli.py`.

**INFERENCE.** There are two valid but separate runtime topologies: a legacy autonomous generic-capability loop and a narrow governed one-shot mutation seam. G2.4 must bridge them intentionally rather than assuming that either is already the other.

## 4. Current Autonomous Loop

| Question | Current answer |
|---|---|
| Where is a goal created? | **FACT:** `build` receives `goal` and constructs `LoopContext`; each iteration constructs `RunContext(goal_text=context.goal)`. |
| Where is planning performed? | **FACT:** `Coordinator.run` calls `DefaultPlanner.create_plan`; if memory and `AdaptivePlanner` are composed, it may derive an adapted plan. |
| Where is the LLM invoked? | **FACT:** not in the canonical autonomous factory or loop. `GatewayRuntime.decide` is a separate explicit composition path. |
| Where is `EngineeringDecision` produced? | **FACT:** only on successful `GatewayRuntime.decide`; no autonomous-loop call path reaches it today. |
| Where is `ChangeProposal` produced? | **FACT:** only in `DecisionToChangeProposalTranslator.translate`; no autonomous-loop call path reaches it today. |
| Where is mutation authorized? | **FACT:** only in `GovernedMutationRuntime.authorize`/`mutate`; no autonomous-loop call path reaches it today. |
| Where is mutation executed? | **FACT:** the legacy path uses generic capabilities. The governed path executes through `GovernedMutationRuntime`; the two are not currently integrated. |
| Where is verification performed? | **FACT:** G2.3.1 verifies proposal postconditions after write; the legacy coordinator invokes `DefaultValidator` on generic step results. Neither is a first-class engineering-objective verifier. |
| Where is reflection performed? | **FACT:** after every Chief run, `AutonomousLoopRuntime` creates `ReflectionContext` and calls `ReflectionRuntime.reflect`. |
| Where is memory written? | **FACT:** immediately after reflection, `MemoryRuntime.store_reflection` writes one coarse memory entry. |
| Where is continue/stop decided? | **FACT:** `CompletionEngine.evaluate` produces `LoopDecision`; the loop also stops on oscillation, recovery abort, approval pause, or max iterations. |
| What survives between iterations? | **FACT:** `LoopResult.iterations`, in-memory plan signatures for oscillation detection, stored reflection memory, and the original `LoopContext` goal/metadata. |
| What is lost between iterations? | **FACT:** `LoopIteration` does not carry decisions, proposals, authorizations, receipts, verification artifacts, context/snapshot identities, provider budgets, or typed stop reasons. |

**FACT.** The current loop has states `RUNNING`, `REFLECTING`, `REPLANNING`, `WAITING_APPROVAL`, `COMPLETED`, `FAILED`, and `ABORTED`; it has `max_iterations` and plan-signature oscillation detection. See `src/eag/autonomous/models.py` and `runtime.py`.

**FACT.** The current completion policy bases continuation on generic `RunResult`, reflection review score, critical findings, iteration count, and `max_iterations`. It does not inspect governed decisions, proposals, authorizations, receipts, verification evidence, or context freshness. See `src/eag/autonomous/completion.py`.

## 5. Gap Analysis

**FACT.** A `ChiefRun` records one `RunContext`, one plan, generic step results, checkpoints, metrics, outcome, and error. `RunResult` carries a plan and generic step results. These types do not contain proposal history, mutation receipt history, verification artifacts, reflection artifacts, a repository-context chain, budget ledger, or typed governed stop reason. See `src/eag/chief/runtime/models.py`.

**FACT.** `ReflectionContext` currently accepts a `run_result`, optional review report, optional benchmark result, and metadata. `MemoryRuntime.store_reflection` stores a coarse success/failure summary from that reflection. Neither type includes governed mutation evidence. See `src/eag/reflection/models.py` and `src/eag/memory/runtime.py`.

**INFERENCE.** The minimum gap is a combination of: deterministic state machine, immutable execution state, first-class verification contract, legal failure transitions, evidence-bound replanning input, proposal/mutation/verification history, budget accounting, fresh-context policy, and typed terminal reasons.

**RECOMMENDATION.** G2.4 should treat the existing autonomous loop as precedent for iteration, reflection, memory, completion, and approval; it should treat G2.3.2 as the sole authorized per-step mutation pipeline. Neither runtime should be silently expanded into the other.

| Missing capability | Demonstrated source gap | Design response |
|---|---|---|
| Governed iteration controller | **FACT:** G2.3.2 workflow is single-shot; legacy loop has no governed workflow composition. | **RECOMMENDATION:** deterministic state machine with guarded transitions. |
| Cross-iteration ledger | **FACT:** `LoopIteration` stores identifiers, timing, and success only. | **RECOMMENDATION:** immutable iteration evidence records and execution context. |
| Objective verification | **FACT:** receipt verification only proves written content; legacy validator only handles step success/retry. | **RECOMMENDATION:** typed verification request/result distinct from mutation receipt. |
| Safe failure continuation | **FACT:** receipt failure stages exist but have no loop transition semantics. | **RECOMMENDATION:** deterministic terminal or reflection/replanning transition table. |
| Fresh evidence | **FACT:** context assembler exposes `ensure_fresh` and `rebuild_if_stale`, but the autonomous factory does not compose it. | **RECOMMENDATION:** refresh context before every new decision and reject stale proposal inputs. |
| Budget ledger | **FACT:** loop has only `max_iterations`; gateway limits apply to a single request. | **RECOMMENDATION:** explicit run-wide budget aggregate. |

## 6. G2.4 Objective

**RECOMMENDATION.** G2.4 should establish one bounded, evidence-driven trajectory of individually governed actions:

```text
Goal -> trusted context -> plan -> governed decision -> one proposal
     -> policy -> one-time authorization -> one mutation receipt
     -> objective verification -> deterministic transition
     -> reflection/replanning only when permitted -> next iteration or terminal stop
```

**RECOMMENDATION.** A G2.4 implementation is successful only when the controller can prove why it entered each state, why it continued or stopped, which bounded evidence it carried forward, and that no iteration bypassed G2.3.1/G2.3.2 policy or authorization.

## 7. Execution State Machine

### Proposed deterministic state graph

```text
[RECOMMENDATION: G2.4 state machine]

CREATED
  -> CONTEXT_ASSEMBLING
  -> PLANNING
  -> DECIDING
  -> PROPOSING
  -> APPROVAL_PENDING (only when trusted approval policy requires it)
  -> AUTHORIZING
  -> MUTATING
  -> VERIFYING
      -> COMPLETED                 [objective verification passes]
      -> REFLECTING                [verification fails and replan budget remains]
      -> FAILED                    [verification failure is not recoverable]

REFLECTING
  -> REPLANNING
  -> CONTEXT_ASSEMBLING            [new iteration, fresh evidence]
  -> FAILED                        [reflection fails or no legal next action]

Any nonrecoverable policy, authorization, provider, context, or budget failure
  -> FAILED or ABORTED

APPROVAL_PENDING -> AUTHORIZING | ABORTED
```

**RECOMMENDATION.** `CREATED`, `COMPLETED`, `FAILED`, and `ABORTED` are terminal-or-entry control states. All other states must have exactly one deterministic owner and an immutable evidence update before transition.

| State | Allowed input | Output / persisted evidence | Legal next states | Illegal transition examples | Failure behavior | Budget effect |
|---|---|---|---|---|---|---|
| `CREATED` | trusted goal and execution policy | execution ID, initial budget snapshot | `CONTEXT_ASSEMBLING`, `ABORTED` | direct `MUTATING` | invalid goal/policy aborts | none |
| `CONTEXT_ASSEMBLING` | workspace identity, goal, prior redacted evidence | snapshot/context fingerprints, truncation metadata | `PLANNING`, `FAILED` | reuse stale context for `DECIDING` | context error/staleness fails current run | one context build |
| `PLANNING` | goal, memory retrieval, prior verified evidence | non-executable plan or planning rationale | `DECIDING`, `FAILED` | planner to `AUTHORIZING` | no legal plan fails | one planning cycle |
| `DECIDING` | fresh `EngineeringContext`, trusted gateway policy | `EngineeringDecisionResult`, usage/trace | `PROPOSING`, `FAILED` | LLM-selected state transition | provider/schema/policy failure fails by default | one decision attempt and usage debit |
| `PROPOSING` | successful decision, trusted workspace state, intent policy | one proposal or sanitized translation violation | `APPROVAL_PENDING`, `AUTHORIZING`, `FAILED` | multiple proposals in G2.4 initial slice | translation failure fails | one proposal slot |
| `APPROVAL_PENDING` | validated proposal and trusted approval mode | approval request/decision | `AUTHORIZING`, `ABORTED` | approval directly mutates | denied/expired approval aborts | approval wait budget/time |
| `AUTHORIZING` | validated proposal | one proposal-bound authorization | `MUTATING`, `FAILED` | reused authorization to `MUTATING` | authorization failure fails | one authorization slot |
| `MUTATING` | validated proposal plus unconsumed authorization | redacted mutation receipt | `VERIFYING`, `FAILED` | generic capability dispatch | policy/write/postcondition failure follows receipt map | one mutation slot |
| `VERIFYING` | completed receipt, trusted verification specification | typed verification result | `COMPLETED`, `REFLECTING`, `FAILED` | receipt success equals objective success | verifier error/failure follows policy | one verification slot |
| `REFLECTING` | receipt, verification result, goal, plan/decision references, bounded context | reflection report | `REPLANNING`, `FAILED` | reflection issues new mutation | reflection failure fails | one reflection slot |
| `REPLANNING` | reflection, accumulated evidence, remaining budgets | deterministic next-step decision | `CONTEXT_ASSEMBLING`, `COMPLETED`, `FAILED` | reusing previous authorization/proposal | exhausted/no-safe-next step fails | one replan slot |
| `COMPLETED` | passed objective verification | terminal summary and evidence index | none | any new mutation | none | final |
| `FAILED` | typed failure/stop reason | terminal redacted failure record | none | automatic provider retry | none | final |
| `ABORTED` | user denial/cancel or safety stop | terminal abort record | none | resume without a new governed run | none | final |

**RECOMMENDATION.** The LLM may influence only the advisory decision payload. It must not choose the state, skip a guard, mark verification successful, consume authorization, reset a budget, or select a terminal outcome.

## 8. Execution Context

**FACT.** Existing types already cover parts of the future context: `LoopContext` covers goal and max iterations; `RunContext` covers run identity and metadata; `EngineeringDecisionRequest/Result` cover context, request policy, usage, trace, and policy violation; `ChangeProposal`, `MutationAuthorization`, and `MutationReceipt` cover one governed mutation; `ReflectionContext/Report` and `MemoryEntry` cover reflection and memory.

**RECOMMENDATION.** Do not mutate or overload those models. Add a minimal immutable `GovernedExecutionContext` and append-only `GovernedIterationRecord` that reference existing artifacts by value or redacted identity.

| Context field | Existing reusable contract | G2.4 need |
|---|---|---|
| execution/run ID and goal | **FACT:** `LoopContext.loop_id`, `RunContext.run_id`, and `ChangeProposal.run_id` exist. | **RECOMMENDATION:** one `execution_id`, one stable goal, and per-iteration IDs. |
| iteration number | **FACT:** `LoopIteration.iteration_number` exists. | **RECOMMENDATION:** retain it and bind each evidence record to it. |
| fresh repository evidence | **FACT:** context assembler exposes snapshot and context fingerprints. | **RECOMMENDATION:** persist fingerprints and truncation status, never raw unrestricted repository content. |
| current plan/decision | **FACT:** `Plan` and `EngineeringDecision` exist. | **RECOMMENDATION:** store immutable references/digests plus current phase. |
| active proposal and authorization | **FACT:** proposal and authorization models exist. | **RECOMMENDATION:** allow at most one active pair in the initial model. |
| mutation and verification result | **FACT:** receipt exists; objective verification result does not. | **RECOMMENDATION:** add a typed verification result reference. |
| reflection and memory | **FACT:** report and memory entry IDs exist. | **RECOMMENDATION:** record IDs and sanitized summary metadata. |
| budget / stop reason | **FACT:** per-request gateway policy and loop max iterations exist. | **RECOMMENDATION:** add a typed aggregate budget ledger and typed stop reason. |

**RECOMMENDATION.** The execution context must remain content-redacted by default. It should retain fingerprints, IDs, policy versions, counters, and bounded summaries, not raw provider text, credentials, or unrestricted file content.

## 9. Verification Model

**FACT.** A completed `MutationReceipt` requires `verification_passed=True`, but that verification is fingerprint-based postcondition verification of the proposal’s expected file content. It does not prove the engineering objective. See `src/eag/mutation/models.py` and `runtime.py`.

**FACT.** `DefaultValidator` returns `CONTINUE` for a successful generic capability result, retries a failed step up to its local limit, then aborts. It is not an objective verifier and is not tied to G2.3.2 evidence. See `src/eag/chief/runtime/validator.py`.

**RECOMMENDATION.** Introduce a future `VerificationRuntime` with a typed, trusted `VerificationRequest` and immutable `VerificationResult`. It must distinguish mutation success, verification success, and objective success.

| Result layer | Meaning | Required evidence |
|---|---|---|
| Mutation success | **FACT:** the bounded write completed and the proposal postcondition matched. | `MutationReceipt.result == COMPLETED` and receipt postcondition flag. |
| Verification success | **RECOMMENDATION:** trusted checks against a declared verification specification passed. | verifier ID/version, bounded observations, pass/fail, sanitized reason. |
| Objective success | **RECOMMENDATION:** deterministic completion policy accepts passed verification against the goal’s declared acceptance conditions. | verification result plus budget/state evidence. |

**RECOMMENDATION.** G2.4.2 should begin with injected deterministic verification doubles and fixture-specific pure checks. It must not let an LLM submit arbitrary shell commands, test commands, network calls, or Git operations as verification.

## 10. Failure Model

**RECOMMENDATION.** A failure transition must be chosen from a typed failure map, never from a free-form LLM recommendation.

| Observed failure | Evidence source | Proposed transition | Rationale |
|---|---|---|---|
| Provider timeout/failure | `EngineeringDecisionResult.error` | `FAILED` with `PROVIDER_FAILED` | Avoid silent automatic retries and avoid inventing new decisions. |
| Schema or decision-policy rejection | gateway result / violation | `FAILED` with `POLICY_REJECTED` | A rejected advisory result must not become a replanning bypass. |
| Translation failure | sanitized translation violation | `FAILED` with `TRANSLATION_FAILED` | No proposal exists to authorize or repair. |
| Mutation policy/authorization failure | receipt/failure stage | `FAILED` with corresponding typed reason | Preserves policy and one-time authorization authority. |
| Atomic write/postcondition failure | receipt | `FAILED` with `MUTATION_FAILED` or `POSTCONDITION_FAILED` | Do not retry an authorization or mutate again implicitly. |
| Objective verification failure | future `VerificationResult` | `REFLECTING` only if a replan budget remains | A later iteration must create a new decision, proposal, and authorization. |
| Reflection failure | reflection runtime error | `FAILED` | Reflection must not be silently skipped. |
| Budget exhausted | deterministic budget ledger | `FAILED` with `BUDGET_EXHAUSTED` | Termination is deterministic. |
| Human denial/expiry | approval decision | `ABORTED` | Approval never becomes an authorization bypass. |

**RECOMMENDATION.** “Replanning” must never mean replaying the same proposal, reusing an authorization, editing provider output, or automatic repair. It means returning to fresh-context assembly and producing a newly governed decision under remaining budgets.

## 11. Reflection/Replanning Model

```text
[RECOMMENDATION: only verification failure can enter the first recovery loop]

Completed mutation receipt
        |
        v
Trusted objective verification -- PASS --> COMPLETED
        |
        FAIL (remaining replan budget only)
        v
ReflectionContext adapter
  goal + plan/decision IDs + proposal digest + receipt + verification result
  + bounded fresh repository context + previous redacted iteration evidence
        |
        v
ReflectionRuntime -> ReflectionReport -> MemoryRuntime
        |
        v
Deterministic ReplanningPolicy
        |
        +--> stop/fail/abort
        '--> CONTEXT_ASSEMBLING for a new governed iteration
```

**FACT.** `ReflectionRuntime` only publishes started/completed/failed events around its engine. It does not own replanning. `MemoryRuntime.store_reflection` only stores a reflection-derived entry. See `src/eag/reflection/runtime.py` and `src/eag/memory/runtime.py`.

**RECOMMENDATION.** Add a `GovernedReflectionContext` adapter rather than changing `ReflectionContext` immediately. The adapter should construct the existing reflection input with a synthetic bounded run-result view and include redacted metadata: iteration, goal digest, decision digest, proposal ID/digest, receipt fields, verification result, context fingerprints, and prior iteration summaries.

**RECOMMENDATION.** A future `ReplanningPolicy` should be deterministic and produce only `STOP`, `FAIL`, `REQUIRE_APPROVAL`, or `CONTINUE_WITH_FRESH_DECISION`. `AdaptivePlanner` may advise a new non-executable plan from this evidence; it cannot authorize or execute a mutation.

## 12. Bounded Execution

**FACT.** The existing autonomous loop has `max_iterations`; the gateway has per-request attempt, timeout, token, cost, and fallback settings. Those limits are not aggregated into one governed trajectory budget.

**RECOMMENDATION.** The minimum G2.4 aggregate budget is below. Every debit is recorded before the next transition.

| Budget | Initial recommended boundary | Deterministic terminal reason |
|---|---|---|
| iterations | `max_iterations >= 1` | `ITERATION_BUDGET_EXHAUSTED` |
| decisions | at most one gateway decision per iteration | `DECISION_BUDGET_EXHAUSTED` |
| proposals | at most one translated proposal per iteration | `PROPOSAL_BUDGET_EXHAUSTED` |
| authorizations/mutations | at most one each per iteration | `MUTATION_BUDGET_EXHAUSTED` |
| verification attempts | at most one per completed receipt per iteration | `VERIFICATION_BUDGET_EXHAUSTED` |
| reflections/replans | zero or one after each eligible verification failure | `REPLAN_BUDGET_EXHAUSTED` |
| provider tokens/cost | sum redacted `GatewayUsage` across decisions | `PROVIDER_BUDGET_EXHAUSTED` |
| wall-clock duration | trusted deadline or elapsed limit | `TIME_BUDGET_EXHAUSTED` |
| failures | bounded failure count by category | `FAILURE_BUDGET_EXHAUSTED` |
| approval wait | optional trusted timeout | `APPROVAL_EXPIRED` |

**RECOMMENDATION.** Provider failure must not consume a mutation budget because no proposal exists, but it should consume a decision attempt and may consume time/cost actually recorded by the gateway.

## 13. Multi-Step Mutation Model

**RECOMMENDATION.** G2.4 should use a serial, evidence-gated model, not a batch proposal model.

```text
Iteration N: fresh context -> one decision -> one proposal -> one authorization
           -> one mutation -> one verification -> terminal or reflection

Iteration N+1: only after a legal reflection/replanning transition
           -> fresh context -> new decision -> new proposal -> new authorization
```

**FACT.** G2.3.2’s mutation intent contract is limited to exactly one future bounded file mutation and no dependencies in its initial slice. Its workflow contains one optional proposal and one optional receipt.

**RECOMMENDATION.** Each mutation remains individually proposed, policy-validated, authorization-bound, executed, receipt-producing, and objectively verified. G2.4 must not introduce a “ten proposals then ten mutations” bypass.

## 14. Event Model

**FACT.** Existing event families already cover repository-context assembly/freshness, gateway request/routing/attempt/policy/completion/failure, mutation proposal/authorization/start/completion/rejection/failure, Chief planning/execution/validation/run completion, and reflection started/completed/failed.

**FACT.** `EventBus` is in-process and synchronous; it dispatches only exact event classes and provides no persistence, durability, replay, or cross-process ordering contract. See `src/eag/events/bus.py`.

**RECOMMENDATION.** Reuse existing granular gateway, context, mutation, and reflection events. Add only execution-controller events that express cross-component state and are not otherwise observable.

| Proposed G2.4 event | Why it is needed | Reused evidence |
|---|---|---|
| `GovernedExecutionStarted` | identifies one trajectory and its budgets | execution ID, goal digest, policy versions |
| `EngineeringIterationStarted` | separates serial governed iterations | execution ID, iteration, fresh snapshot/context fingerprints |
| `GovernedProposalTranslated` | distinguishes translation from `MutationProposed`, which occurs at mutation validation | decision digest, proposal ID/digest, redacted target metadata |
| `VerificationStarted` / `VerificationCompleted` | objective verification has no existing event family | verification ID/version, status, sanitized summary |
| `ReplanningStarted` / `ReplanningCompleted` | makes deterministic continuation observable | prior iteration ID, transition reason, next action |
| `EngineeringExecutionCompleted` | records success terminal reason and final evidence index | stop reason, iterations, budgets consumed |
| `EngineeringExecutionAborted` | records denied/user/safety terminal reason | abort reason, active state, safe counters |

**RECOMMENDATION.** Every proposed event must carry `execution_id`, `iteration`, and an event sequence number generated by the state controller. The event bus itself is not a source of durable execution state.

## 15. Memory Integration

**FACT.** `MemoryRuntime` can store and retrieve reflection-derived experience, and `Coordinator` can use one relevant experience to adapt a base plan. Memory entries currently store reflection ID, a coarse summary, tags, and score/outcome metadata.

**RECOMMENDATION.** G2.4 should retrieve memory only during planning/replanning and write memory only after a terminal iteration reflection. Persist redacted, verified lessons; do not store raw provider content, proposed file content, credentials, or unverified claims.

**RECOMMENDATION.** Add execution provenance to future memory metadata: execution/iteration ID, decision/proposal/receipt IDs, verification status, context fingerprint, and policy versions. This is additive evidence, not a MemoryRuntime redesign.

## 16. Adaptive Planner Integration

**FACT.** `AdaptivePlanner.create_plan` delegates to its base planner. `AdaptivePlanner.plan` rewrites a `Plan` with rule-based `PlanStep` insertions and returns an `AdaptivePlan` plus `PlanningDecision`. It has no authorization or filesystem interface. See `src/eag/adaptive/planner.py`.

**RECOMMENDATION.** Use `AdaptivePlanner` only behind a future `ReplanningAdapter`:

```text
Verified evidence + reflection + memory retrieval
        -> ReplanningAdapter
        -> AdaptivePlanningContext / non-executable Plan
        -> deterministic state controller
        -> fresh governed decision request
```

**RECOMMENDATION.** The planner may suggest a next non-executable planning context. It must not receive `MutationAuthorization`, call `GovernedMutationRuntime`, mutate workspace state, or select a state transition.

## 17. Human Approval Seam

**FACT.** The existing autonomous loop can pause at `WAITING_APPROVAL` after completion evaluation. That approval is not wired to the G2.3.1 authorization boundary.

**RECOMMENDATION.** G2.4 should introduce trusted approval policy modes: `AUTOMATIC`, `REQUIRE_APPROVAL`, and `DENIED`. The seam belongs after a proposal has passed deterministic translation/policy prerequisites and before `GovernedMutationRuntime.authorize`.

**RECOMMENDATION.** An approval decision permits entering `AUTHORIZING`; it does not create an authorization, mutate a file, change policy, or alter proposal content. A denial leads deterministically to `ABORTED`.

## 18. Benchmark Strategy

**FACT.** `BenchmarkRunner` already prepares a disposable fixture workspace, executes an injected benchmark executor, evaluates a result, and cleans the workspace in `finally`. See `src/eag/benchmark/runner.py`.

**RECOMMENDATION.** The first G2.4 benchmark must be deterministic and use a scripted/fake governed gateway plus deterministic verifier and reflection engine. It must not require a live provider.

```text
[RECOMMENDATION: deterministic EBS-G24-001]

Fixture:
  main.py          # initial implementation
  tests/test_main.py # static fixture evidence, not executed by an LLM command

Goal:
  "Add the feature and satisfy the fixture acceptance condition."

Iteration 1:
  scripted validated decision -> proposal 1 -> authorization 1 -> mutation 1
  -> deterministic verifier returns FAIL -> reflection -> deterministic replan

Iteration 2:
  scripted validated decision -> proposal 2 -> authorization 2 -> mutation 2
  -> deterministic verifier returns PASS -> COMPLETED
```

| Required deterministic assertion | Expected evidence |
|---|---|
| bounded iterations | exactly two iterations, no third decision |
| proposal governance | exactly two proposals, each with distinct decision/proposal digests |
| authorization integrity | exactly two authorizations, each consumed once only |
| no unauthorized mutation | every changed path maps to a completed receipt |
| verification-driven continuation | first receipt succeeds but first objective verification fails; only then reflection/replanning occur |
| reflection/replanning | exactly one reflection and one deterministic replan between iterations |
| final success | second verification passes and state is `COMPLETED` |
| deterministic stop | completion reason is typed and budget ledger is final |
| fixture isolation | source fixture remains unchanged and temporary workspace is removed |

**RECOMMENDATION.** A future live lane should be separate, opt-in, one provider call per authorized execution, and attempted only after the deterministic benchmark is green. It must not redefine the deterministic contract or be used to force acceptance.

## 19. Safety Invariants

The following are **RECOMMENDATION: non-negotiable G2.4 invariants**. The execution state machine is not an alternate execution route.

```text
LLM_DIRECT_FILESYSTEM_ACCESS=NO
LLM_DIRECT_SHELL_ACCESS=NO
LLM_DIRECT_GIT_ACCESS=NO
LLM_DIRECT_NETWORK_MUTATION=NO
LLM_DIRECT_CREDENTIAL_ACCESS=NO

MUTATION_POLICY=AUTHORITATIVE
AUTHORIZATION=ONE_TIME_AND_PROPOSAL_BOUND
MUTATION=ATOMIC_AND_BOUNDED
VERIFICATION=DETERMINISTIC
PRESERVATION_REQUIREMENTS=ENFORCED
OUTSIDE_WORKSPACE_MUTATIONS=0
```

**RECOMMENDATION.** Additional G2.4 invariants are `ONE_ACTIVE_PROPOSAL_PER_EXECUTION`, `NO_AUTHORIZATION_REUSE`, `NO_AUTOMATIC_PROVIDER_RETRY_AFTER_TERMINAL_FAILURE`, `FRESH_CONTEXT_PER_NEW_DECISION`, `NO_REPLAN_WITHOUT_TYPED_EVIDENCE`, and `NO_COMPLETION_WITHOUT_OBJECTIVE_VERIFICATION_PASS`.

## 20. G2.4 Phasing

**RECOMMENDATION.** The requested phase names are broadly appropriate, with a strict dependency order that keeps generic capability execution separate from governed execution.

| Phase | Scope | Exit evidence | Explicit exclusion |
|---|---|---|---|
| G2.4.1 — Execution State Machine | immutable execution context, typed state/stop/budget model, pure deterministic transition guards, controller events | exhaustive transition tests including illegal transitions and budget stops | no provider, mutation, verifier, or autonomous-factory rewiring |
| G2.4.2 — First-Class Verification | trusted verification specification/runtime protocol, deterministic fixture verifier/result/events | mutation success, verification failure, and objective completion are independently tested | no arbitrary shell or LLM-defined commands |
| G2.4.3 — Failure → Reflection → Replanning | bounded reflection adapter, memory handoff, deterministic replanning policy | verification failure yields one reflected/replanned fresh iteration | no auto-repair, authorization reuse, or provider retry |
| G2.4.4 — Bounded Multi-Step Governed Execution | new public composition root connecting context, gateway interface, translator, mutation runtime, verifier, controller | scripted two-step trajectory with per-step policy/authorization | no migration of legacy generic `build` path by default |
| G2.4.5 — End-to-End Engineering Benchmark | isolated deterministic EBS-G24-001 and separate live-lane design | two-step acceptance fixture and safety counters | no live-provider requirement for deterministic completion |

## 21. Product/UI Implications

**FACT.** No UI is part of this design or G2.4 scope.

**RECOMMENDATION.** Engine runtime requirements are typed state, evidence, budget, approval, and terminal records. A future UI should consume a redacted projection rather than inspect mutable runtime internals.

| Engine runtime requirement | Future UI projection |
|---|---|
| execution state and sequence | current phase and iteration |
| active decision/proposal identity | current task and redacted proposal summary |
| approval mode/request | approval required, approved, denied, or expired |
| receipt and verification result | mutation result, verification result, files changed, safe diff summary |
| reflection/replan result | reflection summary and next action |
| budget ledger | remaining decisions/mutations/time/cost budget |
| terminal reason | final result and stop explanation |

**RECOMMENDATION.** UI display must never become a control path that bypasses policy, authorization, or state-transition guards.

## 22. Risks

| Risk | Rank | Evidence / rationale | Mitigation |
|---|---|---|---|
| generic capability bypass | **CRITICAL** | **FACT:** current Coordinator dispatches `PlanStep.metadata` through generic `CapabilityRuntime`. | **RECOMMENDATION:** distinct governed composition; never route governed mutations through generic capabilities. |
| infinite or oscillating execution | **HIGH** | **FACT:** current loop only has max iterations and plan-signature oscillation detection. | **RECOMMENDATION:** aggregate budgets, typed stop reasons, transition ledger, and no same-proposal replay. |
| stale context/proposal | **HIGH** | **FACT:** context freshness is available but not in autonomous composition. | **RECOMMENDATION:** fresh snapshot/context before each new decision; proposal fingerprint checks remain authoritative. |
| verification ambiguity | **HIGH** | **FACT:** receipt verification and generic step validation do not prove objective success. | **RECOMMENDATION:** first-class trusted verifier and separate objective-completion guard. |
| repeated authorization/mutation | **HIGH** | **FACT:** G2.3.1 authorization is one-time but no execution ledger coordinates iterations. | **RECOMMENDATION:** active-proposal invariant and ledger checks before every authorization. |
| provider instability | **HIGH** | **FACT:** prior EBS-015 attempt timed out before structured output. | **RECOMMENDATION:** typed provider failure terminal state; no automatic retry; deterministic fixtures first. |
| reflection loop or unsafe replan | **HIGH** | **FACT:** current reflection does not own replanning evidence. | **RECOMMENDATION:** permit reflection only after eligible verification failure and require new fresh decision. |
| state corruption | **HIGH** | **INFERENCE:** mutable ad hoc state could desynchronize artifacts across components. | **RECOMMENDATION:** immutable context snapshots plus append-only iteration records. |
| memory contamination | **MEDIUM** | **FACT:** current memory is reflection-derived and coarse. | **RECOMMENDATION:** persist only verified redacted evidence after reflection. |
| partial mutation/rollback complexity | **MEDIUM** | **FACT:** current runtime conditionally restores only postcondition mismatch. | **RECOMMENDATION:** retain G2.3.1 receipt semantics; do not create cross-step transactional rollback in G2.4. |
| event ordering assumptions | **MEDIUM** | **FACT:** EventBus is synchronous but non-durable and exact-type dispatched. | **RECOMMENDATION:** state ledger, not event order alone, is authoritative. |
| concurrent execution | **MEDIUM** | **FACT:** mutation runtime has a process-local lock; no cross-execution scheduler is part of G2.3.2. | **RECOMMENDATION:** initial G2.4 supports one execution per workspace; reject concurrent starts. |
| human approval deadlock | **MEDIUM** | **FACT:** existing loop can wait for approval. | **RECOMMENDATION:** typed expiry and abort transition with no implicit resumption. |
| budget exhaustion | **MEDIUM** | **FACT:** budgets are currently fragmented. | **RECOMMENDATION:** aggregate budget ledger and deterministic final record. |

## 23. Definition of Done

**RECOMMENDATION.** G2.4 is not complete because “the agent ran twice.” It is complete only when deterministic evidence demonstrates all of the following:

1. Every state transition is validated by a pure deterministic transition guard, and illegal transitions are rejected.
2. Each iteration has a fresh or explicitly validated repository-context fingerprint before a new decision.
3. Every mutation has exactly one `ChangeProposal`, policy validation, proposal-bound one-time authorization, receipt, and deterministic postcondition verification.
4. Mutation success, objective verification success, and engineering-objective completion are distinct asserted values.
5. An objective verification failure produces reflection and exactly one legal replanning decision when budget permits.
6. Replanning produces a fresh governed decision; it never reuses a proposal or authorization.
7. Passed objective verification causes `COMPLETED` with a typed terminal reason.
8. Budget exhaustion, policy rejection, authorization failure, provider failure, and user denial cause deterministic terminal states.
9. Unsafe proposals remain blocked and cause zero out-of-workspace, Git, shell, network-mutation, or credential-access effects.
10. The deterministic isolated benchmark proves a multi-iteration failure-to-success trajectory and fixture cleanup.
11. Existing G2.3.1/G2.3.2 safety tests and legacy autonomous tests remain unchanged and green.

## 24. Migration Strategy

**RECOMMENDATION.** Introduce a separate public `create_governed_engineering_execution_composition` only after G2.4.1 through G2.4.3 are deterministically accepted. Do not replace `create_autonomous_engineering_composition` or the current `build` command in the first G2.4 increment.

**RECOMMENDATION.** Migrate in order: pure models and transition tests; trusted verification interface; reflection/replanning adapter; scripted governed composition; deterministic benchmark; only then consider an opt-in product entrypoint. Preserve direct Chief callers and the existing autonomous build path throughout.

**RECOMMENDATION.** No history rewrite, no reclassification of G2.3.2 live acceptance, and no automatic EBS-015 rerun are part of migration.

## 25. Non-Goals

**RECOMMENDATION.** G2.4 does not include arbitrary multi-proposal batches, direct LLM filesystem/shell/Git/network/credential access, provider retry changes, automatic content repair, mutation policy relaxation, authorization relaxation, cross-step transactions, distributed scheduling, persistent orchestration infrastructure, UI implementation, Chat UI integration, or a live-provider benchmark requirement.

**RECOMMENDATION.** G2.4 also does not retrofit the legacy generic `CapabilityRuntime` and `WorkspaceCapability` route into a governed mutation authority.

## 26. Expected Future Files

**RECOMMENDATION.** Names are proposed, not created by this reconnaissance.

| Proposed file or area | Intended responsibility | Earliest phase |
|---|---|---|
| `src/eag/governed_execution/enums.py` | execution states, stop reasons, approval modes | G2.4.1 |
| `src/eag/governed_execution/models.py` | immutable context, budgets, iteration ledger, transition records | G2.4.1 |
| `src/eag/governed_execution/state_machine.py` | pure legal-transition and budget guard | G2.4.1 |
| `src/eag/governed_execution/events.py` | only missing controller/verification/replanning events | G2.4.1–2 |
| `src/eag/governed_execution/verification.py` | trusted verifier protocol and deterministic result model | G2.4.2 |
| `src/eag/governed_execution/reflection_adapter.py` | bounded adapter from governed evidence to reflection input | G2.4.3 |
| `src/eag/governed_execution/replanning.py` | deterministic continuation/replanning policy | G2.4.3 |
| `src/eag/governed_execution/runtime.py` | serial controller composing existing gateway/translator/mutation services | G2.4.4 |
| `src/eag/governed_execution/factory.py` | separate public governed composition root | G2.4.4 |
| `tests/test_governed_execution_state_machine.py` | pure state/illegal transition/budget coverage | G2.4.1 |
| `tests/test_governed_execution_verification.py` | receipt-versus-objective verification coverage | G2.4.2 |
| `tests/test_governed_execution_replanning.py` | failure/reflection/replanning ledger coverage | G2.4.3 |
| `tests/test_ebs_016_governed_execution_loop.py` | future deterministic two-iteration fixture contract | G2.4.5 |
| `tests/fixtures/ebs_016_governed_execution_loop/` | future isolated fixture repository | G2.4.5 |

## Repository Evidence Index

| Evidence source | Audited fact used in this design |
|---|---|
| `src/eag/cli.py` | `build` creates canonical autonomous composition; benchmark CLI uses legacy generic capabilities. |
| `src/eag/autonomous/factory.py` | canonical autonomous composition excludes repository context, gateway, and governed mutation workflow. |
| `src/eag/autonomous/runtime.py` | current loop iteration, reflection, memory, completion, recovery, approval, oscillation, and max-iteration behavior. |
| `src/eag/chief/runtime/runtime.py` and `coordinator.py` | Chief delegates to Coordinator; Coordinator dispatches generic capabilities from plan metadata. |
| `src/eag/chief/runtime/models.py` | current run/plan models omit governed execution ledger evidence. |
| `src/eag/chief/intelligence/gateway/runtime.py` | governed gateway decision/policy/provider lifecycle and per-request budgets. |
| `src/eag/chief/intelligence/gateway/mutation_workflow.py` | single-shot G2.3.2 workflow. |
| `src/eag/context/runtime.py` | fresh, read-only repository context plus snapshot/context fingerprints. |
| `src/eag/mutation/models.py` and `runtime.py` | one-file proposal, one-time authorization, atomic mutation, receipt, and postcondition semantics. |
| `src/eag/reflection/runtime.py`, `models.py`, and `src/eag/memory/runtime.py` | current reflection and memory boundaries. |
| `src/eag/events/bus.py` and `src/eag/mutation/events.py` | synchronous event semantics and reusable mutation lifecycle events. |
| `src/eag/benchmark/runner.py` | isolated fixture lifecycle and cleanup precedent. |

## Design Stop State

```text
G2.4_RECON=COMPLETE
G2.4_DESIGN=COMPLETE
G2.4_IMPLEMENTATION=NOT_STARTED

G2.3.2_COMMIT=84d745314f71d532c89700bc89340412a114f78d
G2.3.2_TAG=v2.3.2-g2.3.2

EBS_015_ACCEPTANCE=NOT_ESTABLISHED
EBS_015_RERUN=NO
EBS_014_RERUN=NO

SOURCE_CHANGES=NONE
TEST_CHANGES=NONE
BENCHMARK_CHANGES=NONE
PROVIDER_CALLS=0
WORKSPACE_MUTATIONS=0
GIT_MUTATIONS=0
COMMITS=0
PUSHES=0
TAGS_CREATED=0
```

**IMPLEMENTATION FACT — G2.4.1 update (21 August 2026).** The approved first increment is now implemented as an isolated `eag.governed_execution` package. It introduces no provider, mutation, verification, reflection, replanning, capability, memory, workspace, Git, shell, network, credential, or composition integration.

## 27. G2.4.1 Implemented Foundation

**IMPLEMENTATION FACT.** The actual state set is `CREATED`, `CONTEXT_ASSEMBLING`, `PLANNING`, `DECIDING`, `PROPOSING`, `APPROVAL_PENDING`, `AUTHORIZING`, `MUTATING`, `VERIFYING`, `REFLECTING`, `REPLANNING`, `COMPLETED`, `FAILED`, and `ABORTED`. This matches the approved design candidate set; no speculative state was added.

| From state | Actual legal next states |
|---|---|
| `CREATED` | `CONTEXT_ASSEMBLING`, `ABORTED` |
| `CONTEXT_ASSEMBLING` | `PLANNING`, `FAILED` |
| `PLANNING` | `DECIDING`, `FAILED`, `ABORTED` |
| `DECIDING` | `PROPOSING`, `FAILED` |
| `PROPOSING` | `APPROVAL_PENDING`, `AUTHORIZING`, `FAILED` |
| `APPROVAL_PENDING` | `AUTHORIZING`, `ABORTED` |
| `AUTHORIZING` | `MUTATING`, `FAILED` |
| `MUTATING` | `VERIFYING`, `FAILED` |
| `VERIFYING` | `COMPLETED`, `REFLECTING`, `FAILED` |
| `REFLECTING` | `REPLANNING`, `FAILED` |
| `REPLANNING` | `CONTEXT_ASSEMBLING`, `FAILED` |
| `COMPLETED`, `FAILED`, `ABORTED` | no legal transitions |

**IMPLEMENTATION FACT.** `GovernedExecutionContext` is a deeply immutable public value: its context and evidence metadata recursively freeze mappings, sequences, sets, and byte arrays, and external mutable aliases cannot alter stored state. It carries a stable execution ID, run ID, goal, current state, iteration, immutable `ExecutionBudget`, append-only `ExecutionTransitionRecord` history, redacted `ExecutionEvidenceRef` values, and typed stop reason. It carries references rather than full decision, proposal, authorization, receipt, verification, or reflection objects.

**IMPLEMENTATION FACT.** The aggregate `ExecutionBudget` contains exactly `max_iterations`, `max_mutations`, and `max_verifications` plus monotonic consumed counters. A non-`CREATED` public context is accepted only when its complete ledger begins at `CREATED`, uses the legal-transition matrix exactly, remains contiguous, and reaches the supplied current state. Its iteration, mutation, and verification counters must equal, respectively, legal entries into `CONTEXT_ASSEMBLING`, `MUTATING`, and `VERIFYING`; context evidence must exactly equal flattened transition evidence. No elapsed-time, provider-token, provider-cost, or retry budget was added because no compatible aggregate abstraction exists in the current runtime and those integrations are outside G2.4.1.

**IMPLEMENTATION FACT.** Actual terminal stop taxonomy is `SUCCESS`, three typed budget-exhaustion reasons, `POLICY_REJECTED`, `AUTHORIZATION_FAILED`, `PROVIDER_FAILED`, `VERIFICATION_FAILED`, `UNRECOVERABLE`, and `USER_ABORTED`. A completed execution requires `SUCCESS`; an aborted execution requires `USER_ABORTED`; a failed execution rejects both of those nonfailure reasons.

**IMPLEMENTATION FACT.** The state machine emits `GovernedExecutionStarted`, `GovernedExecutionTransitioned`, and `GovernedExecutionStopped` only after it has already produced the next immutable context. Event publication is best-effort because the existing synchronous `EventBus` lets handlers raise: observer failure cannot alter accepted transition correctness, but it can cause telemetry loss. G2.4.1 provides no durable event-delivery guarantee.

**IMPLEMENTATION FACT.** Deterministic coverage includes initial state; valid and invalid transitions; strict typed errors; terminal protection; typed stops; monotonic and exhausted budgets; rejected impossible direct reconstruction; rejected illegal, disconnected, terminal-extended, and state-mismatched histories; ledger-derived budget counts; deep alias-safe metadata/evidence immutability; event ordering; observer-failure independence; no operational-runtime imports; and EBS-016’s fake-reference two-iteration lifecycle. EBS-016 represents future decision/mutation/verification/reflection artifacts only; it invokes none of them.

**IMPLEMENTATION FACT.** Existing `LoopState`, `RunState`, `LoopContext`, `RunContext`, `LoopIteration`, mutation contracts, generic CapabilityRuntime path, autonomous factory, and G2.3.2 workflow remain unchanged. The new package is a foundation, not a new operational runtime or final governed autonomous composition.

**RECOMMENDATION.** G2.4.2 may add first-class verification only after G2.4.1 final regression acceptance. G2.4.3–G2.4.5 remain future work.

**IMPLEMENTATION FACT — G2.4.2 update (21 August 2026).** The G2.4.2 trusted verification boundary is implemented as `eag.governed_execution.verification`. It is isolated from the state machine: it returns immutable verification evidence and a pure objective assessment, but never selects, requests, or performs a state transition.

## 28. G2.4.2 First-Class Engineering Verification

**IMPLEMENTATION FACT.** G2.4.2 distinguishes three separate results. `MutationReceipt.result == completed` plus its G2.3.1 postcondition flag is **mutation success**. `VerificationResult.status == passed` is **verification success** for a trusted independent assertion. `ObjectiveAssessment.status == satisfied` is **objective success**, and the pure completion policy produces it only when both mutation success and trusted verification success hold for the same receipt identity.

| Contract | Actual G2.4.2 boundary |
|---|---|
| Trusted specification | Immutable `VerificationSpecification` selected outside provider output. It has an ID, version, confined relative target, one approved check type, and a positive byte bound. |
| Request | Immutable `VerificationRequest` binds one run, one receipt evidence object, one matching target specification, and optional same-run G2.4.1 context. |
| Result | Immutable `VerificationResult` has a verification ID, status, redacted `VerificationEvidence`, and stable typed failure code. |
| Objective policy | `ObjectiveCompletionPolicy` is a pure assessment function. It has no state-machine, reflection, replanning, mutation, or authorization authority. |
| Ledger handoff | A result produces a redacted `ExecutionEvidenceRef` of kind `VERIFICATION`; attaching it to a future state transition remains a later state-machine caller responsibility. |

**IMPLEMENTATION FACT.** The only first-slice checks are exact UTF-8 file content, expected file existence, expected absence, and an expected SHA-256 fingerprint. Each request binds one target path and a maximum readable byte size. The verifier rejects traversal, absolute paths, symlink components, outside-workspace resolution, nonregular targets, and oversize reads. It only reads a confined target; it contains no command, Git, network, credential, provider, workspace-write, authorization, or mutation operation.

**IMPLEMENTATION FACT.** The verifier does not accept a model assertion such as “verification passed.” There is no provider-claim field or arbitrary command field in a verification specification or request. A provider-originated string cannot become a `PASSED` result; only evaluation of the trusted typed assertion can do so.

**IMPLEMENTATION FACT.** EBS-016 now also proves that a receipt whose G2.3.1 mutation postcondition passed can fail a trusted exact-content assertion and therefore yields an objective status of `NOT_SATISFIED`. The fixture uses no real LLM, provider, shell, Git, network, credential, reflection, replanning, second mutation, or autonomous composition.

**IMPLEMENTATION FACT.** G2.4.2 does not modify G2.3.1 mutation policy/authorization/atomic write/receipt behavior, G2.3.2 preservation/gateway behavior, `AutonomousLoopRuntime`, the autonomous factory, CLI build, `Coordinator`, generic `CapabilityRuntime`, or G2.4.1 transition authority.

**RECOMMENDATION.** G2.4.3 remains the first permitted future integration of trusted verification evidence into reflection/replanning. G2.4.4 remains the future bounded governed-execution composition; neither is part of G2.4.2.

**RECOMMENDATION.** Stop after final G2.4.2 validation. A later milestone requires separate approval before adding reflection, replanning, provider, composition, generic-capability, or live-benchmark behavior.


## 29. G2.4.3 Design-Only Reconnaissance — Failure → Reflection → Replanning

**Reconnaissance date:** 21 August 2026
**Audited baseline:** [`v2.4.2-g2.4.2`](https://github.com/MenaYassa/EAG/tree/v2.4.2-g2.4.2) at `de7b35618d02cb7e9f2d879a80c21d723b1febac`
**Scope:** Design only. No G2.4.3 source, test, benchmark, provider, runtime-composition, or publication change is introduced by this section.

### 29.1 Executive Summary

**FACT.** G2.4.1 already provides the immutable `GovernedExecutionContext`, ledger records, legal transition matrix, aggregate iteration/mutation/verification counters, and terminal stop reasons. G2.4.2 adds deterministic verification and a pure objective-completion assessment, but leaves selection and execution of later transitions to a future caller. The existing reflection, memory, adaptive-planning, Chief/Coordinator, and G2.3.2 mutation subsystems exist independently; no current component composes them as one governed lifecycle.

**INFERENCE.** The smallest safe G2.4.3 slice is not a new autonomous runtime. It is a **bounded recovery adapter layer** that admits only one recovery reason in its first slice: a completed G2.3.1 mutation receipt paired with a trusted G2.4.2 verification failure. It produces reflection and redacted memory/planning evidence, applies a deterministic continuation policy, and returns only a typed next-action decision for the existing G2.4.1 state controller. It must not call a provider, translate a proposal, authorize a mutation, or mutate a workspace.

**RECOMMENDATION.** Limit the future deterministic benchmark to `max_iterations=2`, `max_mutations=2`, and `max_verifications=2`. Iteration one may fail trusted objective verification and enter one reflection/replanning cycle. Iteration two must either complete after trusted verification passes or terminally fail with `VERIFICATION_FAILED`. No third iteration, provider retry, automatic repair, proposal reuse, authorization reuse, or reflection-selected state transition is permitted.

> **G2.4.3 boundary.** Reflection is evidence. Memory is evidence. Adaptive planning is advisory intent. A deterministic controller chooses the legal state transition. A new gateway request creates a fresh decision; the existing translator creates a fresh proposal; the existing mutation runtime produces a fresh proposal-bound one-time authorization and executes at most one bounded mutation.

### 29.2 Current-State Facts and Component Map

| Component | Current fact | G2.4.3 relevance | Existing limitation / gap |
|---|---|---|---|
| `GovernedExecutionStateMachine` | **FACT:** The legal matrix permits `MUTATING → VERIFYING → REFLECTING → REPLANNING → CONTEXT_ASSEMBLING`; `VERIFYING` may also enter `COMPLETED` or `FAILED`. | Supplies the sole legal recovery path and immutable history. | It has no reflection, memory, replanning, gateway, or mutation orchestration. |
| `GovernedExecutionContext` and ledger | **FACT:** Public construction validates a contiguous legal history from `CREATED`; counters equal actual entries into `CONTEXT_ASSEMBLING`, `MUTATING`, and `VERIFYING`; evidence is flattened from records. | Prevents a recovery loop from resetting iteration, mutation, or verification counts. | No first-class reflection/replanning input/output contracts are attached yet. |
| `ExecutionBudget` | **FACT:** It has only max/used iteration, mutation, and verification counters. | Exactly sufficient for the first two-iteration recovery slice. | No reflection/replan counter exists; G2.4.3 must derive the one permitted recovery from ledger state and remaining iteration budget rather than add speculative budgets. |
| `VerificationRequest`, `VerificationResult`, and objective policy | **FACT:** The verifier is read-only and returns an immutable pass/fail result; objective satisfaction requires matching mutation and verification success. | Determines whether recovery is eligible. | It does not choose `REFLECTING`, write memory, or request a new decision. |
| `ReflectionRuntime` / `ReflectionEngine` | **FACT:** `reflect()` publishes started/completed/failed events and delegates to a `ReflectionEngine`. | Produces a `ReflectionReport` from an adapter-built `ReflectionContext`. | Current context is generic and its default engine reads coarse `run_result`, review, and benchmark fields rather than governed receipt/verification evidence. |
| `MemoryRuntime` | **FACT:** `store_reflection()` stores a `MemoryEntry`; retrieval builds at most one relevant `EngineeringExperience` from stored entries. | Can persist and retrieve normalized reflection-derived evidence. | Stored summary/tags/metadata are coarse and omit execution iteration, receipt, verification, and fresh-context provenance. |
| `ExperienceBuilder` / `EngineeringExperience` | **FACT:** Existing code converts reflection findings and recommendations, or memory entries, into immutable lessons and experience. | Provides planner-consumable experience. | It derives project type/outcome from generic summaries and scores, not governed verification evidence. |
| `AdaptivePlanner` / `PlanningDecision` | **FACT:** `AdaptivePlanner.plan()` is an in-memory rule-based transformation from `AdaptivePlanningContext` plus a base `Plan` to `AdaptivePlan` and explainable `PlanningDecision`. | May produce advisory next-planning intent from supplied experience. | It does not build a gateway request, refresh context, choose a governed state, authorize, or mutate. |
| `Coordinator` / `ChiefRuntime` | **FACT:** Coordinator optionally retrieves one memory experience, adapts a generic plan, then executes generic capabilities. | Shows an existing memory-to-plan seam. | Its generic capability route is not the G2.3.2 governed decision/mutation route and must remain separate. |
| `AutonomousLoopRuntime` | **FACT:** It runs Chief, reflects, stores memory, evaluates generic completion/recovery, and may loop under a coarse `max_iterations`. | Serves only as precedent for separation of iteration/reflection/memory. | It has no governed receipt/verification ledger, trusted context refresh, proposal/authorization freshness, or G2.3.2 workflow composition. |
| `GovernedDecisionMutationWorkflow` | **FACT:** It executes exactly one gateway decision, one translation, and one mutation-runtime call. | Remains the per-iteration governed mutation seam. | It owns no verification-to-reflection continuation and cannot create iteration two by itself. |
| `DecisionToChangeProposalTranslator` / `TrustedWorkspaceState` | **FACT:** Translation requires request snapshot/context fingerprints to match trusted runtime bindings and derives a fresh proposal/precondition from bounded current-state reads. | Enforces fresh request-to-proposal bindings for every iteration. | The future controller must recreate both the request and trusted state; it cannot replay the prior inputs. |
| `MutationAuthorization` / `MutationReceipt` | **FACT:** Authorization binds proposal digest, target, workspace/snapshot fingerprints, and policy version; a receipt records one terminal mutation outcome. | Enforces proposal-bound, one-time mutation authority. | No cross-iteration owner currently prevents a caller from mistakenly attempting stale evidence; G2.4.3 needs explicit freshness checks. |
| Existing events | **FACT:** Context, gateway, mutation, reflection, and G2.4.1 transition events are synchronous and non-durable. | Provide best-effort observations only. | Event order/delivery cannot be used as execution authority or durable recovery state. |

### 29.3 Actual Data Flow and Architectural Gap

**FACT.** The legacy autonomous path is:

```text
LoopContext
  → ChiefRuntime / Coordinator generic plan and capability execution
  → RunResult
  → ReflectionContext(run_result)
  → ReflectionRuntime
  → MemoryRuntime.store_reflection
  → generic CompletionEngine / RecoveryEngine
  → next legacy Chief run or terminal loop outcome
```

**FACT.** The current governed per-mutation path is:

```text
EngineeringDecisionRequest + TrustedWorkspaceState
  → GovernedDecisionMutationWorkflow
  → gateway decision validation
  → DecisionToChangeProposalTranslator
  → fresh ChangeProposal
  → existing policy / one-time authorization / bounded mutation
  → MutationReceipt
```

**FACT.** The G2.4.2 path is:

```text
Completed MutationReceipt + trusted VerificationSpecification
  → VerificationRequest
  → DeterministicVerifier
  → VerificationResult
  → ObjectiveCompletionPolicy.assess
  → immutable objective assessment only
```

**INFERENCE.** No current runtime accepts the tuple `(GovernedExecutionContext, MutationReceipt, VerificationResult, ObjectiveAssessment)`, determines that failure is eligible for recovery, adapts reflection input, records a provenance-bound lesson, obtains advisory plan intent, and returns a deterministic continuation decision. That missing controller-adjacent contract is the G2.4.3 boundary.

**RECOMMENDATION.** Do not connect `AutonomousLoopRuntime` or `Coordinator` directly to the G2.4 state machine. Add a narrow adapter path that consumes the existing contracts above and emits only typed evidence plus a deterministic next-action policy outcome. G2.4.4, not G2.4.3, remains the earliest allowed milestone for an opt-in serial runtime that executes the whole state path.

### 29.4 Required G2.4.3 Contracts

| Missing contract | Required responsibility | Must not do |
|---|---|---|
| `GovernedReflectionInput` | Immutable, redacted input binding execution ID, run ID, iteration, goal digest, prior evidence refs, receipt identity/outcome, verification result identity/status, objective status, and fresh-context fingerprints. | Contain raw provider output, proposed content, credentials, absolute paths, or mutable operational handles. |
| `GovernedReflectionAdapter` | Convert trusted governed evidence to existing `ReflectionContext` plus a bounded synthetic run-result view whose `outcome` reflects objective verification failure. | Treat a reflection recommendation as state authority or mutation authority. |
| `GovernedReflectionOutcome` | Bind `ReflectionReport`, optional `MemoryEntry` / `EngineeringExperience` references, and redacted diagnostics to the same execution/iteration. | Store memory without reflection success or cross execution/iteration provenance. |
| `ReplanningInput` | Immutable evidence bundle: prior ledger context, reflection outcome, provenance-filtered memory experience, remaining budget facts, and fresh-context requirement. | Include previous proposal or authorization as reusable action inputs. |
| `ReplanningPolicy` | Pure deterministic policy returning exactly `CONTINUE_WITH_FRESH_DECISION`, `FAIL`, or `ABORT`; first slice does not require approval. | Call providers, select mutation content, mutate, consume budgets, or silently retry. |
| `ReplanningOutcome` | Explainable policy outcome with stable reason code, selected action, and evidence refs for `REFLECTING → REPLANNING` transition. | Embed a new `EngineeringDecision`, `ChangeProposal`, or authorization. |
| `GovernedMemoryProvenance` | Redacted metadata schema for execution/iteration, reflection ID, receipt ID, verification ID/status, objective status, snapshot/context fingerprints, and policy versions. | Turn memory into a source of authoritative authorization or raw workspace content. |
| Fresh-iteration request factory | Later G2.4.4 owner will rebuild repository context, engineering context, decision request, and trusted workspace state after `CONTEXT_ASSEMBLING`. | Reuse the prior request, decision, proposal, authorization, or stale fingerprints. |

### 29.5 Bounded Failure → Reflection → Replanning Flow

**RECOMMENDATION.** The first permitted recovery predicate is evaluated only by trusted code:

```text
eligible_for_recovery =
    receipt establishes mutation success
    AND verification.status == FAILED
    AND objective.status == NOT_SATISFIED
    AND context.state == VERIFYING
    AND context.iteration == 1
    AND context.budget.iterations_remaining >= 1
    AND context.budget.mutations_remaining >= 1
    AND context.budget.verifications_remaining >= 1
```

A failed mutation receipt, policy rejection, authorization failure, provider failure, malformed verification request, verifier confinement failure, stale context, reflection failure, or exhausted budget is **not** an eligible recovery trigger in the first slice.

```text
Iteration 1
  fresh context → plan intent → fresh governed decision → fresh proposal
  → fresh authorization → mutation receipt (completed)
  → trusted verification result (failed)
  → objective assessment (not satisfied)
  → VERIFYING → REFLECTING
  → governed reflection adapter → ReflectionRuntime → ReflectionReport
  → provenance-bound MemoryEntry / EngineeringExperience
  → REFLECTING → REPLANNING
  → deterministic ReplanningPolicy(CONTINUE_WITH_FRESH_DECISION)
  → REPLANNING → CONTEXT_ASSEMBLING

Iteration 2
  fresh context and trusted bindings → fresh plan intent → fresh decision
  → fresh proposal → fresh authorization → second mutation
  → second trusted verification
  → passed objective assessment → VERIFYING → COMPLETED
```

**FACT.** The current G2.4.1 legal matrix already admits each arrow above: `VERIFYING → REFLECTING`, `REFLECTING → REPLANNING`, `REPLANNING → CONTEXT_ASSEMBLING`, and the normal forward path. Entering the second `CONTEXT_ASSEMBLING` consumes the second iteration budget; entering the second `MUTATING` and `VERIFYING` consumes the second mutation and verification counters.

**RECOMMENDATION.** Evidence must be attached at the following transition points.

| Transition | Required evidence ref(s) | Deterministic authority |
|---|---|---|
| `MUTATING → VERIFYING` | redacted `MUTATION_RECEIPT` | Existing G2.3.1 receipt. |
| `VERIFYING → REFLECTING` | `VERIFICATION` result ref and objective-assessment summary | Trusted verifier and pure completion policy. |
| `REFLECTING → REPLANNING` | `REFLECTION` report ref; optionally a provenance-bound memory-entry digest | Reflection adapter/runtime after successful report creation. |
| `REPLANNING → CONTEXT_ASSEMBLING` | deterministic `ReplanningOutcome` ref and prior-evidence index | Pure replanning policy, not an LLM or planner. |
| iteration-two `CONTEXT_ASSEMBLING → PLANNING` | fresh snapshot/context fingerprints and new context reference | Repository-context assembler. |
| iteration-two `PLANNING → DECIDING` | advisory `PlanningDecision` / plan reference | Planner output only; state controller selects the transition. |

### 29.6 Freshness and Non-Reuse Guarantees

| Artifact | Required G2.4.3 guarantee | Existing supporting control | Remaining implementation gap |
|---|---|---|---|
| Repository context | Rebuild a fresh selected context before iteration-two decision; persist new snapshot/context fingerprint refs. | `RepositoryContextAssembler.assemble_selected()` is read-only and produces current fingerprints. | No governed owner invokes it after replanning. |
| Engineering decision | Issue a fresh request with fresh context and a new request/decision identity. | Gateway validates a decision independently for each request. | No recovery flow currently creates the second request. |
| Change proposal | Translate only the new successful decision against new `TrustedWorkspaceState`; proposal must have a new ID/digest. | Translator validates request/trusted fingerprint equality and reads current target state. | No cross-iteration proposal uniqueness invariant is implemented. |
| Authorization | Authorize only the fresh proposal; require its digest and fresh fingerprints. | `MutationAuthorization` is proposal-digest-bound and one-time. | Future controller must reject prior authorization IDs explicitly. |
| Receipt and verification | Bind iteration-two verification to iteration-two receipt identity. | G2.4.2 objective policy already requires receipt identity match. | Future ledger-to-verifier caller must attach the correct refs. |
| Reflection/memory | Bind reflection and memory provenance to the failed iteration and execution. | Reflection and memory have run/reflection IDs. | They lack execution/iteration/verification provenance fields. |

**RECOMMENDATION.** The first implementation must reject a continuation if any new decision request carries the prior context fingerprint, prior decision digest, prior proposal ID/digest, prior authorization ID, or an incompatible receipt/verification identity. Equality is not automatically stale in every theoretical case, but the controller must require a newly assembled context artifact and newly allocated request/decision/proposal/authorization identities regardless of equal content fingerprints.

### 29.7 Budget and Terminal Semantics

**FACT.** The implemented budget contract has exactly iteration, mutation, and verification limits. It consumes them only when the state machine enters `CONTEXT_ASSEMBLING`, `MUTATING`, and `VERIFYING`. No reflection/replanning/provider/cost/time budget is implemented in G2.4.1.

**RECOMMENDATION.** Preserve that scope. G2.4.3 must not add speculative budget categories. The first recovery policy derives its boundedness from the existing ledger and permits recovery only from the first iteration with all three remaining counters sufficient for iteration two.

| Condition observed at `VERIFYING` or recovery boundary | Required result | Terminal reason / effect |
|---|---|---|
| Iteration-one objective pass | `VERIFYING → COMPLETED` | `SUCCESS`. |
| Iteration-one objective fail and all iteration-two capacity remains | `VERIFYING → REFLECTING → REPLANNING → CONTEXT_ASSEMBLING` | One recovery cycle only. |
| Iteration-one objective fail but no capacity for a second mutation or verification | `VERIFYING → FAILED` | `VERIFICATION_FAILED` or the existing specific budget-exhaustion reason when a gated entry is attempted. |
| Reflection adapter/runtime fails | `REFLECTING → FAILED` | `UNRECOVERABLE`; no silent skip or replan. |
| Replanning policy yields `FAIL` | `REPLANNING → FAILED` | `UNRECOVERABLE` with a stable sanitized policy reason. |
| Iteration-two objective fail | `VERIFYING → FAILED` | `VERIFICATION_FAILED`; no `REFLECTING` edge in the first slice. |
| Stale/invalid fresh bindings or gateway/translation/policy/authorization failure in iteration two | Existing normal failure edge to `FAILED` | Existing typed `POLICY_REJECTED`, `AUTHORIZATION_FAILED`, `PROVIDER_FAILED`, or `UNRECOVERABLE` as applicable. |

### 29.8 Reflection, Memory, and Adaptive-Planning Boundaries

**FACT.** `DefaultReflectionEngine` treats `run_result.outcome == "failure"` as generic execution failure and does not inspect governed receipt or verification fields. `MemoryRuntime.store_reflection()` derives a coarse goal from `run_result.summary`, a summary from the first reflection strength, and success/failure from overall score. `ExperienceBuilder` and `AdaptivePlanner` can transform those results into lessons, experience, rules, and explainable planning decisions, but none has mutation authority.

**INFERENCE.** A direct call to existing reflection with an ordinary successful mutation-shaped `RunResult` would not reliably express the authoritative fact that objective verification failed. A direct use of generic `Coordinator.run()` after adaptive planning would route through generic capabilities rather than the governed decision/mutation workflow.

**RECOMMENDATION.** The future adapter should supply a small synthetic, immutable reflection view whose outcome is `failure` because the **objective** failed, not because the first mutation receipt failed. Its `summary` and metadata must be bounded/redacted and state that mutation postcondition passed while trusted objective verification failed. The adapter should preserve the source verification result reference outside the generic reflection engine rather than asking the engine to interpret raw receipt content.

**RECOMMENDATION.** Persist only a deterministic lesson derived from verified facts, for example: `objective verification failed for expected content/fingerprint after a completed bounded mutation; assemble fresh context and request a new governed decision`. The lesson must include provenance IDs/fingerprints and may inform planning, but no reflection recommendation or memory lesson may become mutation content, authorization, or a direct transition request.

**RECOMMENDATION.** The adaptive-plan difference required by the benchmark must arise from an explicit supplied `EngineeringExperience` / `PlanningInsight` / `PlanningRule` chain, and the resulting `PlanningDecision` must record applied/ignored rules. The state controller treats it as advisory plan intent only; a fresh governed gateway validation remains authoritative for the second decision.

### 29.9 Safety Analysis

| Risk | Rank | Existing control | Remaining gap | Recommended G2.4.3 control |
|---|---|---|---|---|
| Stale context | High | Context assembler exposes fresh snapshot/context fingerprints; translator verifies bindings. | No recovery owner rebuilds context. | Require a new assembled context artifact and trusted bindings before iteration-two decision. |
| Stale proposal | High | Proposal binds decision and fingerprints. | Ledger does not yet track active/retired proposal IDs across recovery. | Reject prior proposal ID/digest in `ReplanningInput` and require a new translation result. |
| Stale authorization | High | Authorization is one-time and proposal-digest-bound. | No cross-iteration anti-reuse policy. | Record authorization ref; reject any reuse and authorize only the new proposal. |
| Repeated mutation | High | Existing mutation budget and one-shot workflow. | No recovery owner ties second mutation to exact second iteration. | Existing counter plus one-recovery policy; require distinct receipt/proposal/authorization identities. |
| Budget reset | High | Context validates counters against immutable ledger. | A new controller could create a new context incorrectly. | Continue only from existing context; no reconstruction with reset counters or new execution ID. |
| Reflection hallucination | High | Reflection is not currently an execution authority. | Generic engine may emit broad advice. | Treat report text as non-authoritative evidence; continuation only from typed verification/budget policy. |
| Memory poisoning | High | Memory is separate from mutation. | Retrieval is coarse goal-prefix matching and metadata is generic. | Query/select only execution-provenance-bound entries for the current recovery; retain external memory as non-authoritative advisory evidence. |
| Adaptive planner overreach | High | Planner has no filesystem/authorization interface. | Coordinator can execute generic capabilities after planning. | Keep planner behind a governed replanning adapter; never invoke Coordinator generic execution path. |
| Verification spoofing | High | Verifier ignores LLM claim fields and objective policy checks receipt identity. | Future adapter could attach mismatched refs. | Require receipt/verification identity match in recovery input and ledger evidence. |
| Terminal-state bypass | High | Legal transition matrix has no terminal successors. | Future caller could misuse context construction only if invalid ledger accepted. | Reuse validated context and state machine only; direct reconstruction continues to validate full history. |
| Iteration bypass | High | Iteration increments only on `CONTEXT_ASSEMBLING`. | Future runtime may attempt fresh decision without entry. | Permit new decision only after recorded iteration-two context assembly. |
| Event failure | Medium | State exists before best-effort event publication. | Telemetry can be lost. | Controller state/ledger remains authoritative; never infer recovery from events. |
| Partial mutation | Medium | Existing receipt/atomic mutation and rollback behavior. | Recovery policy must not misclassify failed/rolled-back mutation as eligible. | Eligibility requires completed receipt and trusted objective failure only. |
| Failed replanning | Medium | `REPLANNING → FAILED` is legal. | No policy contract yet. | Stable `FAIL` outcome and `UNRECOVERABLE`; no fallthrough to generic loop. |
| Second verification failure | High | `VERIFYING → FAILED` is legal. | Current matrix also permits `REFLECTING`. | First-slice policy must prohibit recovery after iteration two and select `FAILED(VERIFICATION_FAILED)`. |

### 29.10 Deterministic EBS-017 Design

**RECOMMENDATION.** EBS-017 is a deterministic fixture benchmark, not a live-provider test. It uses a scripted gateway result source, a deterministic reflection engine or fixed reflection report, an isolated in-memory memory store, a deterministic adaptive-planning rule chain, the existing G2.3.1 mutation runtime, and G2.4.2 `DeterministicVerifier`. It does not invoke real providers, shell, Git, network, credentials, EBS-014, or EBS-015.

| Stage | Scripted evidence | Required deterministic assertion |
|---|---|---|
| Fixture start | One bounded source file plus declared acceptance condition. | Disposable workspace only; initial fingerprint recorded. |
| Iteration 1 context | Fresh snapshot/context fingerprints. | Ledger enters `CONTEXT_ASSEMBLING`, consuming iteration one. |
| Iteration 1 governed mutation | Scripted valid decision A, translated proposal A, authorization A, completed receipt A. | Decision/proposal/authorization/receipt A identities are recorded and authorization A is consumed once. |
| Iteration 1 objective verification | Trusted exact-content/fingerprint specification deliberately fails after receipt A succeeds. | Mutation success is distinct from verification/objective failure; ledger enters `REFLECTING`. |
| Reflection and memory | Fixed reflection output plus provenance-bound lesson/experience. | Reflection/memory evidence references bind to execution ID, iteration one, receipt A, and verification A. |
| Adaptive replanning | Supplied experience produces a known planning insight/rule and explainable planning decision. | Adapted plan differs from base plan because of supplied evidence, not hidden state or hardcoded executor branching. |
| Iteration 2 context | Freshly assembled artifact with a new request/decision identity. | Ledger enters `CONTEXT_ASSEMBLING` again; no counters reset. |
| Iteration 2 governed mutation | Scripted valid decision B, proposal B, authorization B, receipt B. | All B identities differ from A; new trusted bindings are used; A proposal/authorization are never accepted. |
| Iteration 2 verification | Trusted verification passes for receipt B. | Objective assessment is satisfied and ledger enters `COMPLETED(SUCCESS)`. |
| Fixture cleanup | Benchmark runner cleanup path. | Fixture source unchanged; temporary workspace removed. |

**RECOMMENDATION.** The EBS-017 negative case repeats the first failed verification/recovery path, then makes iteration-two objective verification fail. It must assert `VERIFYING → FAILED`, terminal `VERIFICATION_FAILED`, exactly two iterations, exactly two mutation/verification entries, exactly one reflection/replanning cycle, and no third decision/proposal/authorization/mutation/verification attempt.

### 29.11 Migration and Coexistence Strategy

**FACT.** `AutonomousLoopRuntime` and its canonical factory remain the existing public Gen1 execution path. They are already published and must not be retrofitted with G2.4.3 state-machine, gateway, or mutation behavior.

**RECOMMENDATION.** G2.4.3 implementation should add pure adapters and contracts only. It should not alter the autonomous factory, `AutonomousLoopRuntime`, `ChiefRuntime`, `Coordinator`, generic `CapabilityRuntime`, CLI build path, G2.3.1 policy/authorization runtime, or G2.3.2 workflow semantics.

**RECOMMENDATION.** A new opt-in governed composition is required eventually, but it belongs to G2.4.4. That future composition may own the serial state controller and call the existing one-shot `GovernedDecisionMutationWorkflow` once per legal iteration. It must not replace the existing autonomous composition.

### 29.12 Future File Forecast

| Future file or area | Why it is needed | Earliest milestone |
|---|---|---|
| `src/eag/governed_execution/reflection.py` | Immutable governed reflection input/outcome, synthetic reflection-view adapter, and redacted evidence conversion. | G2.4.3 |
| `src/eag/governed_execution/replanning.py` | Pure deterministic `ReplanningPolicy`, action/reason enums, and replanning input/outcome. | G2.4.3 |
| `src/eag/governed_execution/events.py` additions | Optional redacted reflection/replanning controller events only if current transition events cannot provide adequate observability. | G2.4.3 |
| `tests/test_governed_execution_reflection_replanning.py` | Pure adapter, recovery-eligibility, non-reuse, second-failure, and planner-advisory tests. | G2.4.3 |
| `tests/test_ebs_017_governed_recovery_loop.py` | Deterministic two-iteration fixture benchmark and bounded negative case. | G2.4.3 or G2.4.5 if runtime composition is intentionally deferred |
| `src/eag/governed_execution/runtime.py` | Serial execution owner that composes fresh context, gateway, mutation workflow, verifier, and G2.4.3 adapters. | G2.4.4 only |
| `src/eag/governed_execution/factory.py` | Opt-in governed composition root. | G2.4.4 only |

### 29.13 G2.4.3 Non-Goals

G2.4.3 does not implement a Chat UI, frontend, streaming UI, unrestricted autonomous loop, provider retries, arbitrary shell execution, Git automation, generic capability replacement, parallel workers, multi-agent orchestration, human approval UI, production deployment, EBS-014/EBS-015 reruns, live-provider acceptance, or the G2.4.4 serial governed runtime. It does not change mutation policy, preservation policy, authorization semantics, gateway behavior, autonomous composition, Chief/Coordinator generic execution, or G2.4.1/G2.4.2 behavior.

### 29.14 G2.4.3 Definition of Done

**RECOMMENDATION.** Implementation should not start until the following is approved as a contract:

1. A trusted recovery-eligibility predicate admits only completed-mutation plus failed trusted-objective-verification evidence under sufficient remaining existing budgets.
2. Reflection input/output, memory provenance, replanning input/output, and reason codes are immutable, redacted, and independently testable.
3. Reflection, memory, and adaptive planning have no state-transition, provider, proposal, authorization, mutation, filesystem, shell, Git, network, or credential authority.
4. Replanning returns only a deterministic next action; the state machine remains the sole legal-transition authority.
5. A recovery continuation always reaches a new `CONTEXT_ASSEMBLING` entry before a second decision and records fresh context evidence.
6. Iteration-two decision, proposal, authorization, receipt, and verification identities are new and cannot equal/reuse iteration-one authorities.
7. Existing iteration, mutation, and verification counters remain ledger-derived and cannot reset.
8. The deterministic EBS-017 success path proves failure → reflection → memory/experience → adaptive intent → fresh second governed mutation → verification success → `COMPLETED`.
9. The negative EBS-017 path proves a second verification failure terminates safely with no third iteration.
10. G2.4.1, G2.4.2, G2.3.1, G2.3.2, autonomous, normal EBS, static-analysis, and whitespace regressions remain green without live-provider calls.

### 29.15 G2.4.3 Reconnaissance Stop State

```text
G2.4.3_RECON=COMPLETE
G2.4.3_DESIGN=COMPLETE

SOURCE_CHANGES=NONE
TEST_CHANGES=NONE
BENCHMARK_CHANGES=NONE
PROVIDER_CALLS=0
WORKSPACE_MUTATIONS=0
GIT_MUTATIONS=0
COMMITS=0
PUSHES=0
TAGS_CREATED=0

G2.4.4=NOT_STARTED
```

**RECOMMENDATION.** Stop after this design record. G2.4.3 implementation requires separate approval and must remain limited to the pure reflection/replanning contracts and deterministic evidence boundaries defined here.


## 30. G2.4.3 Implemented Bounded Reflection → Replanning Evidence Contracts

**IMPLEMENTATION FACT (21 August 2026).** G2.4.3 implements small immutable evidence contracts in `eag.governed_execution.reflection` and `eag.governed_execution.replanning`. It does **not** implement a serial governed runtime, invoke a provider, compose the legacy autonomous path, call the G2.3.2 workflow, authorize a proposal, or mutate a workspace. Those operational responsibilities remain deferred to G2.4.4.

### 30.1 Delivered Governed Reflection Boundary

**IMPLEMENTATION FACT.** `GovernedReflectionInput` accepts only a G2.4.1 context in `REFLECTING`, a structurally verified completed receipt surface, a failed G2.4.2 `VerificationResult`, and a matching unsatisfied `ObjectiveAssessment`. It requires exact run, receipt, verification, proposal, authorization, and ledger-evidence identity matches before existing reflection can be called.

**IMPLEMENTATION FACT.** `GovernedReflectionAdapter` creates a bounded generic `ReflectionContext` with a synthetic failed run-result view. It exposes redacted execution ID, iteration, receipt ID, verification ID, objective status, context artifact ID/fingerprint, and policy version only. It calls the existing `ReflectionRuntime` and returns a provenance-bound `GovernedReflectionOutcome`; it does not choose a state, create a gateway request, translate, authorize, mutate, verify, or terminally classify the execution.

**IMPLEMENTATION FACT.** `GovernedMemoryProvenance` binds execution, run, iteration, receipt, verification, reflection, context artifact, context fingerprint, policy version, and contract version. `GovernedMemoryEvidence` binds the existing immutable `EngineeringExperience` to that same provenance. The adapter uses the existing `ExperienceBuilder`; it does not alter `MemoryRuntime` or add memory retrieval to `AdaptivePlanner`.

### 30.2 Delivered Replanning and Freshness Boundary

**IMPLEMENTATION FACT.** `ReplanningInput` accepts only a G2.4.1 context in `REPLANNING` and requires reflection and memory provenance to match the current execution and iteration exactly. It carries prior decision, proposal, authorization, and context-artifact identities exclusively as values that a future iteration must not reuse.

**IMPLEMENTATION FACT.** `ReplanningPolicy` is pure and deterministic. Its outcomes are `CONTINUE_WITH_FRESH_DECISION`, `FAIL`, or `ABORT`. In this slice it permits continuation only for iteration one after failed trusted verification and unsatisfied objective evidence with at least one remaining existing iteration, mutation, and verification capacity. It returns a stable reason code and a redacted `REPLANNING` ledger evidence reference; it consumes no budget and changes no state.

**IMPLEMENTATION FACT.** `FreshIterationArtifacts` plus `ReplanningPolicy.validate_fresh_iteration` reject reused context artifact, decision, proposal, authorization, receipt, verification, and context fingerprint identities. The contract requires new values before any future G2.4.4 owner can proceed through `CONTEXT_ASSEMBLING → PLANNING → DECIDING → PROPOSING → AUTHORIZING → MUTATING → VERIFYING`.

### 30.3 State and Budget Integration

**IMPLEMENTATION FACT.** G2.4.3 adds `MEMORY` and `REPLANNING` as redacted `ExecutionEvidenceKind` values. It does not alter the G2.4.1 state set, legal-transition matrix, context reconstruction validation, terminal protection, or existing budget model. The already-legal path is exercised deterministically:

```text
VERIFYING (objective failed, iteration one)
  → REFLECTING
  → REPLANNING
  → CONTEXT_ASSEMBLING (iteration two)
  → PLANNING → DECIDING → PROPOSING → AUTHORIZING → MUTATING → VERIFYING
  → COMPLETED | FAILED(VERIFICATION_FAILED)
```

**IMPLEMENTATION FACT.** Existing ledger-derived budgets remain authoritative: iteration consumes only on `CONTEXT_ASSEMBLING`, mutation only on `MUTATING`, and verification only on `VERIFYING`. Reflection and replanning cannot reset or consume those counters. The second EBS-017 failure path selects terminal `FAILED(VERIFICATION_FAILED)` and verifies that a terminal context cannot enter a third iteration.

### 30.4 Deterministic EBS-017 and Adversarial Coverage

**IMPLEMENTATION FACT.** `tests/test_ebs_017_governed_recovery_loop.py` is a no-provider deterministic fixture. Its success path creates iteration-one receipt/verification failure evidence, produces provenance-bound reflection and experience, runs the existing pure `AdaptivePlanner` with a supplied experience-driven rule, validates fresh second-iteration identities, and reaches `COMPLETED` only after iteration-two verification passes.

**IMPLEMENTATION FACT.** Its negative path makes iteration-two verification fail, records `FAILED(VERIFICATION_FAILED)`, and asserts there is no legal third `CONTEXT_ASSEMBLING` transition. The fixture uses controlled evidence objects only; it does not call a live provider, shell, Git, network, credential, generic capability mutation, or workspace runtime.

**IMPLEMENTATION FACT.** `tests/test_governed_execution_reflection_replanning.py` covers stale reflection, stale memory binding, stale context fingerprint, previous decision/proposal/authorization/receipt/verification reuse, mismatched receipt/verification, incompatible iteration, budget exhaustion, terminal-state protection, reflection failure propagation, second-iteration provenance protection, and absence of operational mutation imports in the new modules.

### 30.5 Preserved Boundaries and Non-Goals

**IMPLEMENTATION FACT.** G2.3.1/G2.3.2 mutation policy, preservation policy, translation, authorization, receipt behavior, provider behavior, `ReflectionRuntime`, `MemoryRuntime`, `AdaptivePlanner`, `Coordinator`, `ChiefRuntime`, `AutonomousLoopRuntime`, autonomous factory, generic capability runtime, and CLI build path are unchanged.

**IMPLEMENTATION FACT.** The LLM remains unable to select state transitions, declare verification success, reset a budget, reuse a proposal or authorization, call reflection/memory/planning as mutation authority, or create a new mutation path. `MUTATION_POLICY=AUTHORITATIVE`, `AUTHORIZATION=ONE_TIME_AND_PROPOSAL_BOUND`, `MUTATION=ATOMIC_AND_BOUNDED`, `PRESERVATION_REQUIREMENTS=ENFORCED`, and `VERIFICATION=DETERMINISTIC` remain unchanged.

**IMPLEMENTATION FACT.** G2.4.4 is not started. In particular, G2.4.3 does not claim a full autonomous engineering loop or an operational owner for the whole serial lifecycle.
